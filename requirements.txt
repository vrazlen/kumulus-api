# Initialize a new Poetry project in the current directory.
# You can accept the defaults for most prompts.
poetry init

# Add the production dependencies specified in the blueprint
poetry add langchain langchain-openai faiss-cpu sentence-transformers rank-bm25
poetry add geopandas rasterio pydantic-settings "typer[rich]" structlog python-json-logger

# Add the development dependencies into a 'dev' group
poetry add --group dev pytest pytest-mock ruff